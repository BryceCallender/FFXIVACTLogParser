import { NetworkAbilityLine } from "./network-ability-line.model";

export class NetworkAOEAbilityLine extends NetworkAbilityLine {
    constructor(lineContents: string[]) {
        super(lineContents);
    }
}